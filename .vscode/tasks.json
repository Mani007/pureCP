{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run C++ (input.txt → output.txt)",
            "type": "shell",
            "command": "cmd.exe",
            "args": [
                "/c",
                "cd /d \"${workspaceFolder}\" && g++ -std=gnu++17 -O2 -Wall main.cpp -o program.exe && program.exe < input.txt > output.txt"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "presentation": {
                "reveal": "always",
                "clear": true
            }
        }
    ]
}
